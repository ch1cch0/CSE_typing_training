## 웹 서비스 주제

정컴타자연습 : 프로그래머들의 전용 타자연습 및 언어 학습 웹 사이트. 대략적인 기능은 다음과 같다.
1. 타자 연습 : 각 언어별로 자주 사용하는 문장을 타이핑하고 시간당 타수를 측정
2. 퀴즈 : 각 언어별 키워드, 라이브러리 함수 등에 대한 설명을 보고 맞추는 퀴즈
3. 검색 : 위키백과 API를 활용하여 궁금한 내용을 검색할 수 있는 기능
4. 개인 기록 : 회원가입 및 로그인 서비스를 제공, 사용자의 누적 연습 수를 바탕으로 환산한 레벨, 최고타수를 기록
5. 랭킹 시스템 : db에 등록된 사용자들 중 레벨 또는 최고 타수가 가장 높은 몇 명을 랭킹에 등록하여 유저들이 볼 수 있도록하는 랭킹 페이지를 제공

## 주제 선정 배경 및 이유

종종 컴퓨터를 켜놓고 할 일이 없을 때 한컴타자연습을 켜서 타자연습을 하곤 한다. 그런데 일상문 대신에 코딩할 때 자주 사용하는 문장을 연습할 수 있으면 어떨까 하는 생각에서 출발하여 학생들의 코딩능력 향상 및 간단한 심심풀이의 목적으로 이번 프로젝트의 주제를 선정하게 되었다.

## 개발언어 및 사용도구/라이브러리

프론트엔트
- HTML : 웹페이지의 뼈대 및 내용물 구성
- CSS : 웹페이지의 외관(스타일) 디자인
- JS : UI 상호작용 / 타자 속도, 정확도 계산등의 로직 구현

백엔드
- PHP : 사용자 인증, 타자 및 퀴즈 로직 처리 등에 필요한 DB 연동
- Node.js : API를 활용한 검색 서비스 구현

기타
- MySQL(RDBMS) : 사용자 정보(계정 정보, 개인 기록 등) 저장 / 타자 연습 및 퀴즈의 문제 저장
- Docker/Docker Compose(배포) : 쉽고 빠른 운영환경 구성 및 빌드
- Codex(AI Agent) : 초기버전 스켈레톤 코드 구성 및 구현 보조
- GitHub(협업) : 각자의 branch에서 맡은 부분을 수정하고 공동작업시에 수정내용 공유 후 merge
- app.diagrams.net : 개요도 시각화

## 시스템 개요
<img width="743" height="689" alt="개요도(그림)" src="https://github.com/user-attachments/assets/91c86876-72ff-4b7b-aa30-606633edbe31" />

### 이번 프로젝트는 구체적으로 다음과 같은 기능을 수행한다.

**사용자 기능**

	- 로그인 / 회원가입
	- 메인페이지(사용자 정보 / 게임 선택)
	- 타자연습
	- 퀴즈
	- 위키검색
	- 실시간 피드백

**서버 기능**

	- 세션 인증
	- 사용자 정보 조회, 갱신
	- 점수, 레벨, 랭킹 계산
	- 문장, 퀴즈 제공
	- 검색 API 라우팅

**데이터 기능**

	- 회원 정보 저장
	- 기록 저장
	- 문장, 퀴즈 데이터 제공

### 이를 위하여 다음과 같이 시스템을 구성하였다.

**컨테이너 구성**

	Docker, Docker compose를 활용해 메인서비스(Apache + PHP), DB(MySQL) , 검색서비스(Node.js)를 독립된 컨테이너로 구성했다. 

**메인서비스**

	메인 서비스는 PDO를 통해 DB 컨테이너와 통신하며 사용자 인증, 타자 및 퀴즈 서비스, 랭킹 서비스를 제공한다. 
	
	app/의 php 파일들은 각 페이지를 구성한다.
	
	- index.php : 로그인 페이지, 이미 로그인되어 있으면 main.php로 리다이렉트
	- register.php : 회원가입 페이지, 회원가입 이후 자동으로 로그인되며 main.php로 이동
	- main.php : 메인페이지
	- typing.php / quiz.php : 타자연습, 퀴즈 페이지
	- search.php : 위키검색 페이지
	
	app/includes/의 php 파일들은 메인서비스와 DB의 상호작용을 담당한다.
	
	- db.php : db와의 pdo연결을 일괄적으로 처리한다
	- auth.php : 각 페이지에서 필요한 사용자 인증 / 세션 관리를 처리한다
	- functions.php : 레벨, 랭킹, 진행도 등 
	
	기타 파일
	- app/assets/script.js : 각 페이지에 필요한 타수 및 정확도 계산, 레벨 계산, 랭킹 팝업 표시 등의 로직을 담당. 
	- app/assets/style.css : 각 페이지의 스타일 지정
	- app/api/update_score.php : script.js에서 최신화된 사용자의 정보를 db에 업데이트
	- app/api/search_api.php : 검색서비스 컨테이너로부터 받아온 검색 결과를 JSON으로 전달

**DB**

	DB에는 메인서비스를 통해 등록된 사용자의 정보와 db/init.sql에서 등록된 타자 연습 및 퀴즈의 문제 정보가 저장된다. 테이블의 형태는 다음과 같다.
	
	users : 사용자의 정보를 관리, 사용자 인증정보는 auth.php, 점수 정보는 update_score.php를 통해 업데이트된다
	- id : 자동으로 할당되는 기본 키
	- username : 사용자 이름
	- password_hash : auth.php에서 해싱되어 저장된 사용자 비밀번호
	- total_typing : 사용자가 타자 연습한 총 문장 수
	- total_quiz : 사용자가 푼 퀴즈 문제 수
	- level : total_typing과 total_quiz를 바탕으로 계산된 레벨
	- highest_speed : 타자 연습 최고 기록
	- created_at : 계정 생성 일시
	
	typing_sentences : 타자 연습에서 사용되는 문장들, functions.php를 통해 전달된다
	- id : 기본 키
	- language : 프로그래밍 언어
	- sentence : 문장 내용
	
	quiz_questions : 퀴즈에서 사용되는 문제들, functions.php를 통해 전달된다
	- id : 기본 키
	- language : 프로그래밍 언어
	- keyword : 키워드 (정답)
	- description : 키워드에 대한 설명 (문제)

**검색서비스**

	검색 서비스는 다음과 같은 흐름으로 구현된다.
	- search-service/src/index.js : 위키피디아 API를 호출해 제목, 요약, URL을 정제해 반환
	- app/api/search_api.php : 로그인 여부를 확인하고 반환받은 결과를 JSON으로 전달
	- app/assets/script.js : JSON을 바탕으로 search.php 페이지에 검색 결과를 나타냄

## 팀 멤버 별 담당 업무
	- 강범수 : UI 개발 및 수정, DB 및 도커 구축, error 이벤트 처리 등 백엔드 로직 개발, 전체 시스템 개요도 작성
	- 정시원 : HTML, CSS 등 기초 프론트 개발, 퀴즈 로직 및 검색 API 기능 구현, 영상 발표 담당
	- 조현우 : index.php, main.php 등 도메인 화면 및 로그인 로직 구현, 타자 연습 로직 구현, 랭킹 시스템 구현
